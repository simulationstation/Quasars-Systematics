"""Minimal, vendored subset of the entropy-horizon reconstruction utilities.

This module is intentionally small and exists to keep the QS repo self-contained for
the paper/repro artifacts we run here (without depending on /home/primary/PROJECT).
"""

